#!/usr/bin/env node
var debug = require('debug')('ik_front');
var app = require('../app');

app.set('port', process.env.PORT || 8084);

var server = app.listen(app.get('port'), function () {
    console.info(debug);
    debug('Express server listening on port ' + server.address().port);
});
